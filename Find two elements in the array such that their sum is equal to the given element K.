// You are using Java
import java.util.*;
import java.util.Scanner;
class main
{
    public static void main(String[]args)
    {
        Scanner s=new Scanner(System.in);
        int n=s.nextInt();
        int []arr=new int[n];
        for(int i=0;i<n;i++)
        {
            arr[i]=s.nextInt();
        }
        int target=s.nextInt();
        Set<Integer> seen = new HashSet<>();
        boolean found =false;
        for(int i=0;i<n;i++)
        {
            int num=arr[i];
            int number=target-num;
            if(seen.contains(number))
            {
                found = true;
                break;
            }
            seen.add(num);
        }
        if(found)
        {
            System.out.println("Array has two elements with given sum "+target);
        }
        else
        {
            System.out.println("Array doesn't have two elements with given sum "+target);
        }
    }
}
